# Build authoritative cse-3rd-sem-timetable(1).json from user specifications
import json
import os
from pathlib import Path

ROOT = Path(__file__).resolve().parent.parent
JSON_PATH = ROOT / "cse-3rd-sem-timetable(1).json"

# We extract and save the complete json
# Let's verify and save the data
