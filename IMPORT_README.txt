This bundle replaces the earlier incomplete timetable export.

IMPORTANT:
- It contains all 24 sections A1-L2.
- It includes BET-I, which was previously omitted by a numeric-only course-code parser.
- It normalizes the timetable's visually rendered BCSE-50lL as BCSE-501L.
- Use the supplied importer with your existing local backend/.env.
- Do not commit backend/.env or service-role credentials.
